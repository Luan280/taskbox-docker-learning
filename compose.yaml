services:
  web:
    build: .
    ports: ["127.0.0.1:5000:5000"]
    volumes:
      - ./app.py:/app/app.py
    command: flask --app app run --debug --host=0.0.0.0 --port=5000
    environment:
      TASKBOX_MESSAGE: ${TASKBOX_MESSAGE}
